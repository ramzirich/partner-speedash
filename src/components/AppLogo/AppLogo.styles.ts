import { StyleSheet } from 'react-native';

/**
 * Styles for <AppLogo>. Kept in a separate file per the project convention.
 */
export const styles = StyleSheet.create({
  logo: {
    alignSelf: 'center',
  },
});
